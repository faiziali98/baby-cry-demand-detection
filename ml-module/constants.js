const imgWidth = 200;
const imgHeight = 200;
const numClasses = 3;
const inputShape = [imgWidth, imgHeight, 3];

module.exports = {
    inputShape, 
    numClasses,
    imgHeight,
    imgWidth
};